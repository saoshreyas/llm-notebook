Audience: {{ audience }} (programmers and non-programmers — use simple language).

=== PIPELINE (parsed structure) ===
{{ ast_summary }}

Explain:
1. What this pipeline does end-to-end (2–4 sentences).
2. Each node in order: what it does and what it passes to the next step.
3. Which nodes are code (Python generated from intent) vs LLM (prompt only).

Use short bullet lists. No code fences unless quoting an intent line.
