# NotebookLM Frontend

React + Vite + Tailwind Jupyter-style UI for Workflow DSL.

```bash
npm install
npm run dev
```

Proxies `/api` → `http://localhost:8000`. Start the API with `notebooklm app run` first.
